// Test-only stub for Next's `server-only` package. Loaded via the require
// hook in `test-setup.cjs` so library modules guarded with
// `import "server-only"` can be exercised under `node --test`.
"use strict";
module.exports = {};
