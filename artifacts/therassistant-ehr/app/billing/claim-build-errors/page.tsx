import ClaimBuildErrorsClient from "./ClaimBuildErrorsClient";

export const metadata = {
  title: "Claim Build Errors",
};

export default function ClaimBuildErrorsPage() {
  return <ClaimBuildErrorsClient />;
}
