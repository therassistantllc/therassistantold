# Concept dictionary seed

Canonical source of truth for the starter concepts loaded by the
`20260520040000_concept_dictionary.sql` migration. The migration mirrors these
files; if you edit a concept here, update the migration (or a follow-up
migration) so the database stays aligned.

Future CIEL/SNOMED imports will land alongside these as additional JSON files.
